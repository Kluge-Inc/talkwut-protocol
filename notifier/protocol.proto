message Notification {
  required string category = 1;
  required string message = 2;
  optional string url = 3;
}

message Email {
  repeated string mail = 1 [packed=true];
  required Notification message = 2;
}
